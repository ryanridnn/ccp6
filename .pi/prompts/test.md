---
description: Do tasks based on md
---

You will need to test something for the user. Please before executing the task, generate a plan as a table on what you about to test, please make list of items, with its expectation, then once ready call plan_confirmation tool. Once user confirmed execute the task sequentially.

For testing, use agent-browser mcp, and remember to always user HEADED browser (set header to true on opening). Usually the local development is in localhost:3000

Once test is finished make a report, generate that table with the test results.

For further context, the user prompt is: $ARGUMENTS
